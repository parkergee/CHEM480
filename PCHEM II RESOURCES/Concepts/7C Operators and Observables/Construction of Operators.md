How do you set up a operator corresponding to a given observable?

Observables are represented by operators built from the following position and linear momentum operators:

xhat = x*
phat(x) = hbar/i d/dx

